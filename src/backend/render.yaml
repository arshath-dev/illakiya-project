# Exported from Render on 2026-03-08T09:15:33Z
version: "1"
projects:
- name: My project
  environments:
  - name: Production
    services:
    - type: web
      name: illakiya-project
      runtime: python
      repo: https://github.com/arshath-dev/illakiya-project
      plan: free
      envVars:
      - key: PYTHON_VERSION
        sync: false
      region: oregon
      buildCommand: pip install -r requirements.txt
      startCommand: uvicorn app:app --host 0.0.0.0 --port $8000
      autoDeployTrigger: commit
      rootDir: src/backend
